import * as Styled from './Chevron.styles';
import type { ChevronProps } from './Chevron.types';

export function Chevron(props: ChevronProps) {
    return <Styled.Chevron {...props} />;
}

export default Chevron;
